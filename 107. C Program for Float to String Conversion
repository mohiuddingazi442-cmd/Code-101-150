#include <stdio.h>

int main() {
    float num;
    char str[50];

    printf("Enter a float number: ");
    scanf("%f", &num);

    sprintf(str, "%f", num);   // float to string

    printf("String value: %s\n", str);

    return 0;
}
