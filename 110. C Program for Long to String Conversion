#include <stdio.h>

int main() {
    long num;
    char str[50];

    printf("Enter a long number: ");
    scanf("%ld", &num);

    sprintf(str, "%ld", num);   // long to string

    printf("String value: %s\n", str);

    return 0;
}
