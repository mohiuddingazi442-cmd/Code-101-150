#include <stdio.h>

int main() {
    int num;
    char ch;

    printf("Enter an integer value: ");
    scanf("%d", &num);

    ch = (char)num;   // int to char conversion

    printf("Character equivalent: %c\n", ch);

    return 0;
}
