#include <stdio.h>

int main() {
    int decimal;

    printf("Enter a decimal number: ");
    scanf("%d", &decimal);

    printf("Hexadecimal equivalent: %X\n", decimal); // Uppercase
    // printf("Hexadecimal equivalent: %x\n", decimal); // Lowercase

    return 0;
}
