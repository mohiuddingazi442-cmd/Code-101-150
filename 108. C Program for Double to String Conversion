#include <stdio.h>

int main() {
    double num;
    char str[50];

    printf("Enter a double number: ");
    scanf("%lf", &num);

    sprintf(str, "%lf", num);   // double to string

    printf("String value: %s\n", str);

    return 0;
}
